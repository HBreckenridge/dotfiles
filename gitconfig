[user]
	name = hbreckenridge
	email = hans@upstrem.dev
[color]
	ui = auto
[merge]
	conflictStyle = diff3
[core]
	editor = code --wait
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
